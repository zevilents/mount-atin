local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Rayfield Example Window",
   Icon = 0,
   LoadingTitle = "Rayfield Interface Suite",
   LoadingSubtitle = "by Sirius",
   ShowText = "Rayfield",
   Theme = "Default",
   ToggleUIKeybind = "K",
   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false,
   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil,
      FileName = "Big Hub"
   },
   Discord = {
      Enabled = false,
      Invite = "noinvitelink",
      RememberJoins = true
   },
   KeySystem = false,
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided",
      FileName = "Key",
      SaveKey = true,
      GrabKeyFromSite = false,
      Key = {"Hello"}
   }
})

-- Menggunakan tab yang sudah Anda berikan
local Tab = Window:CreateTab("Tab Example", 4483362458)

-- Buat bagian baru di dalam tab yang sudah ada
local PlayerSection = Tab:CreateSection("Player Functions")

-- Tambahkan slider walkspeed
PlayerSection:CreateSlider({
   Name = "WalkSpeed",
   Range = {16, 100}, -- Atur rentang nilai
   Increment = 1, -- Peningkatan per geseran
   Suffix = "Speed", -- Teks di akhir nilai slider
   CurrentValue = 16, -- Nilai awal
   Flag = "WalkSpeed", -- Nama untuk konfigurasi yang disimpan
   Callback = function(value)
     game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
   end
})
